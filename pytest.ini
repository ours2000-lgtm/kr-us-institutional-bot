[pytest]
addopts = -ra
testpaths =
    tests_mvp
pythonpath =
    .
    src

markers =
    e2e: end-to-end trace validation tests
    health: health subsystem tests (recovery, transitions, invariants)